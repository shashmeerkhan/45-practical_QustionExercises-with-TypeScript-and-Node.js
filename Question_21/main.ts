// They think of something you could store in a TypeScript Object.
//  Write a program that creates Objects containing these items.
interface addmision {
  name: string;
  FatherName: string;
  age: number;
  class: number;
  entrytest: boolean;
}

let addmision = {
  name: "Ali Raza",
  FatherName: "Shehzad Khan",
  age: 10,
  class: 5,
  entrytest: true,
};

console.log(addmision);
